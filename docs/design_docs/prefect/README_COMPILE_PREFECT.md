# TODOS / references / gotchas while using prefect

- UI / API can be run behind reverse proxies or custom domain DNS; need to setup some configs!
- https://docs.prefect.io/v3/manage/server/index#the-prefect-database
- https://docs.prefect.io/v3/develop/settings-and-profiles

For now, in prod -> access prefect UI via SSH tunnel forward porting and don't expose prefect UI port to external traffic!




