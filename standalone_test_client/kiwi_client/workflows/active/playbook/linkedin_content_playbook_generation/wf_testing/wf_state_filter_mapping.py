"""
State filter mapping for LinkedIn Content Playbook Generation workflow.

This mapping defines which parts of the workflow state to include in state dumps
for easier debugging and analysis.
"""

# State filter mapping - empty dict means no filtering (include all state)
state_filter_mapping = {}
